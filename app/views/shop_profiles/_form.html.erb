<div class="user-form">

  <h2>My Shop</h2>

  <%= form_with(model: shop_profile, local: true) do |form| %>
    <% if shop_profile.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(shop_profile.errors.count, "error") %> prohibited this shop_profile from being saved:</h2>

        <ul>
        <% shop_profile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :business_name %>
      <%= form.text_field :business_name %>
    </div>

    <div class="field">
      <%= form.label :about %>
      <%= form.text_area :about %>
    </div>

    <div class="field">
      <%= form.label :location %>
      <%= form.text_field :location %>
    </div>

    <div class="field">
      <%= form.label :contact_number %>
      <%= form.text_field :contact_number %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>

</div>